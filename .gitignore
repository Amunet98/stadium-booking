# Local environment — never commit real credentials
.env
.env.*
# ...but the *.example templates carry no secrets and must stay tracked.
!*.example

# Provider CA certificates fetched for a local connection test
*.pem
*.crt
*.key

# Dependencies
/vendor/
composer.lock

# PHP / editor noise
*.log
.php-cs-fixer.cache
.phpunit.result.cache
.idea/
.vscode/
*.swp
.DS_Store

# Python bytecode from docs/extract-screenshots.py
__pycache__/
*.pyc
